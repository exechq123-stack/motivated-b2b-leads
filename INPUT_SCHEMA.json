{
  "title": "Hacker News Lead Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQueries": {
      "title": "Search Queries",
      "type": "array",
      "description": "Keywords to search for in HN posts/comments",
      "editor": "stringList",
      "default": [
        "lead generation",
        "marketing automation",
        "sales process",
        "customer acquisition",
        "built a tool for",
        "struggling with growth",
        "how do you qualify leads",
        "what CRM do you use"
      ],
      "prefill": [
        "lead generation",
        "marketing automation",
        "customer acquisition"
      ]
    },
    "searchType": {
      "title": "Search Type",
      "type": "string",
      "description": "What to search: comments, stories, or both",
      "enum": ["comments", "stories", "both"],
      "default": "both",
      "editor": "select"
    },
    "maxResultsPerQuery": {
      "title": "Max Results Per Query",
      "type": "integer",
      "description": "Maximum number of items to fetch per search query",
      "default": 50,
      "minimum": 1,
      "maximum": 200
    },
    "daysBack": {
      "title": "Days Back",
      "type": "integer",
      "description": "How many days back to search (1-365)",
      "default": 30,
      "minimum": 1,
      "maximum": 365
    }
  },
  "required": []
}
